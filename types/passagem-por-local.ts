export type PassagemPorLocalProxy = {
    local: string
    oportunidades: number
    unicos: number
}


export type PassagemPorLocal = {
    local: string
    oportunidades: number
    unicos: number
}